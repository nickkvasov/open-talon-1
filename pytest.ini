[pytest]
asyncio_mode = auto
testpaths =
    tests
    test/infrastructure
norecursedirs =
    .git
    .pytest_cache
    .venv
    api-gateway
    test/api-gateway
markers =
    integration: requires running infrastructure (docker-compose)
addopts = -m "not integration"
