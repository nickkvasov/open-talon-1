# task-router

Placeholder package for a possible future extraction of task-creation and routing logic.

Current status:

- this package is not part of the local runtime
- `./open-talon start` does not launch it
- current task creation and routing behavior still lives in `services/core-collab` and the collaboration routes in `services/gateway-edge`

Treat this directory as a non-runtime placeholder, not as an active architectural component.
